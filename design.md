# تصميم واجهة ريموكيبورد مزخرف

## الرؤية والنطاق

ريموكيبورد مزخرف هو تطبيق أندرويد باللغة العربية يقدّم لوحة مفاتيح عربية متعددة اللغات مع مركز تحكم واضح وسريع. يعتمد التصميم على وضعية عمودية بنسبة 9:16 وعلى وصول العناصر الرئيسية بإبهام واحد. تتبع الواجهة مرجع اللقطات المرفقة في التباين الداكن، ترتيب العربية من اليمين إلى اليسار، وأيقونات وظيفية واضحة، مع هوية حديثة وليست نسخة مطابقة لأي تطبيق آخر.

سيُبنى المشروع بطبقتين متكاملتين: تطبيق إدارة وإعدادات خفيف، ومكوّن إدخال أندرويد أصلي (`InputMethodService`) ليعمل كلوحة مفاتيح فعلية. تتطلب طبقة لوحة المفاتيح عملية بناء أندرويد أصلية مستقلة لضمان استهداف Android 5.0 (API 21)؛ أما واجهة Expo فهي مركز الإدارة والمعاينة المتاح ضمن بيئة المشروع.

## قائمة الشاشات

| الشاشة | المحتوى الرئيسي | الوظائف |
| --- | --- | --- |
| البداية | حالة تفعيل الكيبورد، خطوات قصيرة، معاينة ثيم نشط | فتح إعدادات النظام، تشغيل المعاينة، اختيار الثيم |
| الإعدادات | أقسام اللغة والكتابة والزخرفة والثيمات والإيموجي والحافظة | انتقال واضح إلى كل إعداد مع وصف مختصر |
| معاينة الكيبورد | صف اقتراحات، صف أدوات، تخطيط عربي حقيقي، صف سفلي كامل | اختبار الحروف، التبديل بين الصفحات واللغات، إدخال الرموز والتشكيل |
| الثيمات | بطاقات ثيم داكن شبابي، وردي نسائي، ذهبي إسلامي، وفاتح | تطبيق فوري للثيم وحفظ الاختيار محليًا |
| الزخرفة | حقل نص ونتائج أنماط زخرفية آمنة | نسخ النتيجة، حفظ المفضلة، إدراجها من الكيبورد |
| الحافظة | قائمة نصوص منسوخة مع معاينة لعدد الأحرف | تثبيت، نسخ، حذف، وتحديد حد التخزين المحلي |
| الاستديو | تبويبات للإيموجي والملصقات ومحرر ملصق بسيط | اختيار إيموجي، كتابة ملصق نصي، حفظه محليًا |
| الكتابة واللغات | العربية والإنجليزية والرموز، أنظمة الأرقام، المد والتشكيل | اختيار اللغة الافتراضية، ضبط الأرقام، تفعيل الضغط المطول |

## تدفقات المستخدم الأساسية

بعد فتح التطبيق يرى المستخدم بطاقة حالة التفعيل. يضغط «تفعيل الكيبورد» فينتقل إلى صفحة إعدادات لوحة المفاتيح في أندرويد ثم يعود لاختيار ريموكيبورد كلـوحة إدخال نشطة. بعد ذلك، يمكنه اختبار الإدخال من شاشة المعاينة أو من أي تطبيق يدعم الكتابة.

عند الكتابة، يضغط المستخدم حرفًا عربيًا فتُدخل الخدمة النص في الحقل الحالي. يقود الضغط المطول على **ة** إلى شريط الحركات `َ ً ُ ٌ ِ ٍ ْ ّ ٰ`، والضغط المطول على **ت** إلى بدائل المد والتطويل مثل `ـ` و`تـ`. يتيح زر اللغة الانتقال بين العربية والإنجليزية، فيما تتيح المسافة والرجوع والإدخال تنفيذ أوامر محرر النص الأصلية وفق حقل الكتابة المستهدف.

من شريط الأدوات، يختار المستخدم الحافظة أو الإيموجي أو الزخرفة. تحفظ الحافظة النصوص محليًا مع حد عددي قابل للتعديل؛ وتحوّل الزخرفة النص المدخل إلى أنماط Unicode عربية آمنة قدر الإمكان مع خيار النسخ. يعرض زر الصوت واجهة التعرف الصوتي في الجهاز بعد منح الإذن، ثم يُدرج النص الذي تعيده خدمة التعرف؛ ولا تعد الواجهة بدقة مطابقة مضمونة لأن الجودة تعتمد على محرك أندرويد والميكروفون والاتصال.

## القرارات البصرية

| العنصر | الاختيار | الغرض |
| --- | --- | --- |
| الخلفية الأساسية | `#10161F` كحلي فحمي | تباين مريح للواجهة العربية الداكنة |
| السطح والبطاقات | `#182432` و`#233246` | فصل هرمي واضح من دون ضوضاء بصرية |
| اللون الرئيسي | `#5CC8FF` أزرق سماوي | أزرار التفعيل والحالات المختارة |
| إبراز الزخرفة | `#B997FF` بنفسجي ناعم | منح أدوات الإبداع هوية مميزة |
| إبراز الثيم الإسلامي | `#D5AE55` ذهبي معتدل | استخدام خاص ببطاقات المناسبات الإسلامية |
| النص الأساسي | `#F3F7FC` | وضوح عالٍ مع واجهة داكنة |
| المحاذاة | RTL، 20px هوامش، 14–18px زوايا | سهولة قراءة ولمس في وضع اليد الواحدة |

تظل الأزرار التفاعلية بمساحة لمس لا تقل عن 44 نقطة، وتُصمم الصفوف السفلية للوحة المفاتيح بارتفاع ثابت نسبيًا لتقليل الأخطاء. يعتمد تخطيط «الكمبيوتر» على مفاتيح منفصلة ذات تباعد مرئي وحدود دقيقة، ويستخدم رد فعل لمسي وصوت اختياري من إعدادات أندرويد عند الضغط.

## نموذج البيانات المحلي

| الكيان | حقول أساسية | التخزين |
| --- | --- | --- |
| إعدادات الكيبورد | اللغة، نظام الأرقام، الثيم، الصوت، الاهتزاز، المد، ارتفاع المفاتيح | `SharedPreferences` في طبقة IME و`AsyncStorage` في واجهة الإدارة |
| عنصر الحافظة | المعرف، النص، تاريخ الحفظ، مثبّت | `SharedPreferences` مع حد السعة |
| نمط زخرفة | المعرف، الاسم، دالة التحويل، مفضّل | مورّد محلي داخل الحزمة |
| ملصق نصي | المعرف، النص، اللون، الخلفية، الرمز | تخزين محلي كبيانات JSON |
| تخطيط الإدخال | اللغة، الصفحة، المفتاح، بدائل الضغط المطول | مورد Kotlin/XML محلي |

## متطلبات الأداء والخصوصية

يُحفظ محتوى الحافظة والملصقات والإعدادات على الجهاز افتراضيًا، ولا يُرسل النص إلى خادم. تستخدم ميزة الكلام محرك التعرف الصوتي المتاح في الجهاز فقط، وقد يستدعي هذا المحرك الشبكة أو خدمات النظام وفق إعدادات المستخدم. سيُبنى كل مورد للمفاتيح والثيمات محليًا لتقليل الحجم؛ ويمكن لاحقًا فصل حزم الثيمات أو الإيموجي الكبيرة إلى تنزيلات اختيارية إن لزم.
