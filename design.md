# تصميم واجهة REMO PLAYER

## رؤية المنتج

**REMO PLAYER** هو مشغل Android محلي للموسيقى والفيديو يركز على الوصول السريع إلى الملفات الموجودة في الهاتف، ووضوح التحكم أثناء التشغيل، وتجربة داكنة هادئة تناسب الاستخدام الليلي. لا يحتاج المستخدم إلى حساب أو اتصال سحابي؛ إذ تحفظ التفضيلات وقوائم التشغيل على الجهاز فقط.

## مبادئ الواجهة

تُصمَّم التجربة للشاشة العمودية بنسبة **9:16** وللاستخدام بيد واحدة. تُوضَع إجراءات التشغيل الأساسية في النصف السفلي من الشاشة، ويكون شريط التبويب ثابتاً في الأسفل. تُستخدم مساحات لمس لا تقل عن 44 نقطة، ونصوص عالية التباين، وانتقالات قصيرة لا تتجاوز 250 مللي ثانية. اللغة الأساسية العربية مع اتجاه واجهة من اليمين إلى اليسار.

## قائمة الشاشات

| الشاشة | المحتوى الأساسي | الوظائف المتاحة |
|---|---|---|
| الرئيسية | بطاقة «المتابعة من حيث توقفت»، أحدث الملفات، واختصارات الأنواع | استئناف التشغيل والانتقال للمكتبة أو البحث |
| الموسيقى | قوائم الأغاني والألبومات والفنانين | البحث، التشغيل، الإضافة إلى قائمة تشغيل، والفرز |
| الفيديو | قائمة ملفات الفيديو المحلية مع معلومات المدة والنوع | التشغيل، البحث، واستئناف الفيديو |
| شاشة التشغيل الصوتي | الغلاف، اسم المسار، شريط تقدم، تحكم السابق/تشغيل/التالي | تبديل التكرار والخلط، ضبط السرعة، وتقديم أو إرجاع التشغيل |
| شاشة تشغيل الفيديو | مشغل فيديو مع طبقة تحكم بسيطة | تشغيل/إيقاف، ملء الشاشة، السرعة، والترجمة عند توفرها |
| قوائم التشغيل | قوائم المستخدم والمفضلة | إنشاء قائمة، تعديل الاسم، وإدارة العناصر |
| البحث | حقل بحث ومجموعات نتائج للموسيقى والفيديو | فتح النتيجة أو تشغيلها مباشرة |
| الإعدادات | سمة التطبيق، تفضيلات التشغيل، وأذونات المكتبة | تعديل التفضيلات وإعادة فحص المكتبة |

## تدفقات المستخدم الأساسية

| الهدف | التدفق |
|---|---|
| تشغيل أغنية | يفتح المستخدم «الموسيقى» ← يختار أغنية ← تظهر نافذة التشغيل المصغرة ← يفتحها للتحكم الكامل |
| تشغيل فيديو | يفتح المستخدم «الفيديو» ← يختار ملفاً ← تظهر شاشة المشغل ← يستخدم الإيماءات أو عناصر التحكم للتحكم بالتشغيل |
| إنشاء قائمة تشغيل | يفتح المستخدم «قوائم التشغيل» ← يضغط «إنشاء قائمة» ← يكتب الاسم ← يضيف المسارات من قائمة الموسيقى |
| إيجاد ملف | يفتح المستخدم «البحث» ← يكتب جزءاً من الاسم ← يختار أغنية أو فيديو من النتائج |
| متابعة المحتوى | يفتح المستخدم الصفحة الرئيسية ← يضغط بطاقة «المتابعة» ← يستأنف التطبيق من آخر موضع محفوظ |

## الألوان والخطوط

| العنصر | اللون | الغرض |
|---|---|---|
| الخلفية العميقة | `#08111F` | أساس داكن مريح للمكتبة والمشغلات |
| سطح البطاقات | `#111F33` | فصل البطاقات وشريط التشغيل المصغر |
| الأزرق الكهربائي | `#2EC5FF` | إجراء رئيسي، حالة نشطة، ومؤشر التقدم |
| البنفسجي النابض | `#8B5CF6` | لمسات الهوية والتدرجات البصرية |
| النص الأساسي | `#F4F8FF` | عناوين ومعلومات التشغيل المهمة |
| النص الثانوي | `#91A1B7` | البيانات الوصفية والتلميحات |
| النجاح | `#44D7A8` | تأكيدات الحفظ والحالات المكتملة |

تستخدم الواجهة خط النظام العربي في Android لضمان الأداء وتناسق القراءة. يعتمد أسلوب العناوين على وزن عريض، بينما تستخدم معلومات المسارات وزناً عادياً وارتفاع سطر مريح.

## نموذج البيانات المحلي

| الكيان | الحقول الرئيسية |
|---|---|
| MediaItem | المعرّف، العنوان، الفنان، الألبوم، النوع، المسار المحلي، المدة، الصورة المصغرة، نوع الوسيط |
| Playlist | المعرّف، الاسم، تاريخ الإنشاء، عناصر الوسائط المرتبة |
| PlaybackState | العنصر الحالي، الموضع، حالة التشغيل، السرعة، التكرار، الخلط، آخر تحديث |
| AppSettings | السمة، العرض المدمج، إظهار المشغل المصغر، تفضيلات السرعة والتكرار |

## الحدود الإصدارية

تغطي النسخة الأولى قراءة ملفات الوسائط التي يختارها المستخدم أو التي يمكن الوصول إليها بإذن النظام، وتشغيل الفيديو والصوت محلياً، وحفظ تقدم التشغيل وقوائم المستخدم محلياً. لا تتضمن هذه النسخة بثاً من خدمات خارجية أو تنزيل محتوى محمي أو مزامنة سحابية.
