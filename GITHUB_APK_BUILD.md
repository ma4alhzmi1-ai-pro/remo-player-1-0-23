# بناء APK عبر GitHub

يُنشئ سير العمل الموجود في `.github/workflows/android-apk.yml` ملف APK لنظام Android من مصدر REMO PLAYER نفسه، دون إرسال مشروعك إلى خدمة البناء السحابية الخاصة بـExpo.

## متى يبدأ البناء؟

يبدأ البناء تلقائياً بعد كل رفع إلى الفرع `main`. ويمكن أيضاً تشغيله يدوياً من تبويب **Actions** في GitHub، ثم اختيار **Build Android APK** والضغط على **Run workflow**.

## تنزيل الملف

بعد ظهور حالة نجاح البناء، افتح تشغيل البناء ثم ابحث عن قسم **Artifacts**. نزّل العنصر المسمى `REMO-PLAYER-Android-Standalone-APK`، فك ضغطه، ثم ثبّت الملف `app-release.apk` على هاتف Android. يتضمن هذا الملف حزمة JavaScript داخله ولا يحتاج إلى Metro أو اتصال بالكمبيوتر عند الفتح.

## ملاحظة التوقيع

إذا كانت نسخة REMO PLAYER الحالية على الهاتف موقعة بمفتاح مختلف، فقد يطلب Android إزالة النسخة القديمة قبل تثبيت هذه الحزمة. لإتاحة تحديثات متتابعة فوق بعضها من GitHub، يلزم في خطوة لاحقة إعداد مفتاح توقيع خاص ومستمر داخل أسرار المستودع.
